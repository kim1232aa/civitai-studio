# 交接文档 — 2026-09-10（Claude架构师）

本文档基于本轮**独立核实**（git log、进程、测试实跑），不是转述群聊口头结论。写作时刻：origin/main = `abef37a0`。

## 1. 总目标

唯一入口 `http://127.0.0.1:18832/storyboard.html`。借鉴 Seko 式无限画布 + 六家真实前端点击生成闭环（视觉写回、参数出站、任务轮询、媒体回写、点击验收全打通）。硬性口径：

- 禁止直打 `/api/generate`，禁止重复图/重复 prompt
- LoRA 按各家真实 API 全参数点击验收，不发明默认值、不截断 prompt、不静默换模/sibling
- 视觉验收以**原站真实画面**为准，不是内部清单字面（2026-09-10 大佬钉：pi 以后必须原站截图 + 我方现网截图同发对比，不接受只审派工方划定的范围）

**当前总体判定：未通过。** 原因见第 3 节。

## 2. 服务与仓库现状（本轮独立核实）

- 五个 worktree 共享同一个 `.git`（`git worktree list`），不是五份独立 clone：
  - `work-repo/civitai-studio-cloud-main` — 分支 `main`，**这是唯一正确工作目录**，现服 18832 就跑在这里
  - `work-repo/civitai-studio` — 分支 `feat/v0794-prompt-nodes`
  - `work-repo/civitai-studio@p2fix` — 分支 `fix/d-gate-hangups`
  - `work-repo/cs-fix-b1b2` — 分支 `fix/e-gate-b1b2`
- 现服进程：PID `912973`（ppid=15，`python3 server.py`，起于 10:31:02），cwd = `civitai-studio-cloud-main`，`git rev-parse HEAD` = `abef37a0` = `origin/main`，**完全对齐，无漂移**。
- **origin/main 最新提交是 `abef37a0`**（"delete shots from planner and editor with confirmation"），比群聊摘要里记录的 `c2b885a` 新一笔——ASTRA/grok 已经把"真实删除分镜"这条直接推上了 main。**"页头 script 工作区 与 项目面板 story 工作区合一"这条尚未做**（`data-workspace="script"` 和 `data-workspace="story"` 两个标记仍同时存在于代码里，本轮核实过，不是猜测）。

## 3. 本轮清理工作区做了什么

发现并修复了一个**长期误导源**：`work-repo/civitai-studio-cloud`（分支 `feat/cloud-nodes-poc`）曾被多个 Agent 当作"服务仓"cd 进去核对 HEAD，但它其实落后 origin/main 15 个提交、且不含当天任何修复（mojibake、ASTRA 三闸、delete-shot 等全部没有）。这解释了此前反复出现的"HEAD/SHA 对不上、现服未钉到最新"式报错——很可能是核对者站在了这个过期目录里，而不是服务真正运行的 `civitai-studio-cloud-main`。

已执行：
1. `git worktree remove` 删除了 `civitai-studio-cloud`（feat/cloud-nodes-poc），并强删该分支（已确认其提交全部包含在 main 里，无独有内容丢失）。
2. 清掉一个孤立 worktree `/home/ubuntu/temp/d-review-b5431f3`（Sep 7 遗留，游离 HEAD，与本轮工作无关）。
3. `civitai-studio-cloud-main` 里一个未跟踪文件 `scripts/test_upscale_relight.js`（C 组"超清/打光/消除/九宫格/故事/切分"的测试脚手架，本轮已决定 C 组不做）迁出到 `temp/c-group-deferred-20260910/`，不删除，留作 C 组重启时的起点；仓库工作区现在完全干净（`git status` 无输出）。

**以后所有 Agent 核对 HEAD/PID/JS SHA，只认 `work-repo/civitai-studio-cloud-main`，不要再进其它目录。**

## 4. 三条旁支为什么没有"合并"——需要大佬拍板

大佬要求"合并分支"，本轮核实后判定：**不能用 `git merge` 硬合，会破坏 main。** 证据：

| 分支 | 与 main 共同祖先 | 祖先落后 main | 分支自身新增改动 |
|---|---|---|---|
| `feat/v0794-prompt-nodes` (77f9e46) | `46b5544`（2026-09-05） | 171 个提交 | 工作区本身不干净（TODO.md/docs 有未提交改动） |
| `fix/d-gate-hangups` (414f105) | 同上 `46b5544` | 171 个提交 | `storyboard.js` +6622 行、**含 12 张打光预设图**（`light-preset-01..12.jpg`） |
| `fix/e-gate-b1b2` (8dc364d) | 同上 `46b5544` | 171 个提交 | `storyboard.js` +6819 行、同样带打光预设图 |

三条分支都从同一个 9 月 5 日的老祖先分叉，而 main 在那之后已经重写了 `storyboard.js` 大半（171 个提交，含今天的 mojibake/ASTRA 三闸/delete-shot 等）。三条分支各自又在老基础上加了 6600+ 行改动，其中**打光预设图属于已经明确本轮不做的 C 组**（群聊已钉："ASTRA 打光 swatch = C 组，本轮不并"）。

直接 `git merge` 这三条到 main：
- 冲突面是几千行级别，不是可以机械解决的小冲突
- 会把 C 组（打光预设）夹带进来，违反已经拍板的"C 组本轮不做"
- 这三条分支里针对 D 门/E 门的原始修复思路，很可能已经被 main 上后来的直接提交（`0f3e6cd` versionId AIR fallback / Checkpoint 类型闸、`94e9d89` 混合出站失败、`0b35cf1` 缺字段整包失败）用不同写法重新做过一遍——盲目合并有可能把已经验证过的新写法覆盖回旧写法

**这不是例行执行项，是方向性决策，需要大佬明确选一个：**
- (a) 彻底放弃这三条分支（保留只读存档，不再提"合并"）
- (b) 只把 `feat/v0794-prompt-nodes` 里"提示词节点 UI"这部分单独挑出来，针对当前 main 重新实现（不是 git merge，是照着旧分支的思路在新代码上重做）
- (c) 指定专人逐文件人工合并，接受这是一次大工作量的返工

另外 `origin` 上还有 3 条纯远程、无本地 worktree 的旁支（`feat/v0794-prompt-complete`、`fix/storyboard-js-main`、`fix/storyboard-js-quotes`），全部落在同一个 9 月 4-5 日的老祖先附近，情况与上面一致，本轮未动，一并等拍板。

## 5. 测试基线（本轮实跑，非转述）

`PYTHONPATH=. python3 scripts/test_*.py`，共 26 个文件：**16 PASS / 10 FAIL**。

FAIL 逐项归类（已看报错，未深挖修复——分类供后续派工参考）：

- **环境/fixture 缺文件，非代码回归**：`test_air_map`（硬编码 `/workspace/civitai-studio/...` 路径，本机不是这个根）、`test_import_meta`（fixture png 缺失）、`test_nanogpt_media_contract`（引用的 `/out/upload_20260909....jpg` 已被清理）
- **测试硬编码旧缓存戳，随每次 cache-bust 必然失败，非产品回归**：`test_astra3_gates`（断言 `?v=20260910-astra-0163679`，已被后续提交改掉）、`test_storyboard_graph`（**现在只有 14/54，不是群聊摘要里记的 52/54**——`test_v0821k/l/m2` 三个 html-stamp 断言先失败，疑似拖垮了后面的级联；这三个测试需要改成判断内容/行为而不是判断精确版本号字符串，否则每次改 cache stamp 都会假红）
- **测试断言的是旧行为，代码已按大佬要求改成新行为**：`test_p0_wiring` 断言"NanoGPT 超范围 seed 必须拒绝"，但 `0163679` 已经按官方文档撤掉了这条误抄自魔搭的假上限（Nano 官方无 min/max）——测试没跟着更新，需要改断言，不是改代码
- **未验证/需要人工看**：`test_fal_fields`（`fal-ai/flux/schnell` 端点报"没有 duration 不能丢弃"，是 fixture 传错参数还是真 bug，本轮没查）、`test_e5_media_io`（未知后端断言细节不对，群聊里已记为历史非阻塞项）、`test_cloud_nodes_disconnect_race`（`graphEpoch` vs `epoch` 命名不一致，群聊已记为不阻塞）
- **不算失败**：`test_workspace_delete_browser.py` 需要 `--out` 参数，直接跑会报 usage error，不是断言失败

**结论：测试红不能直接当"有回归"，但 `test_storyboard_graph` 从 52/54 掉到 14/54 这件事本身需要有人专门看一次**——不确定是三个 html-stamp 断言的连锁误报，还是真的有東西坏了，本轮没有时间深挖，标记为遗留问题。

## 6. 已完成、有独立证据的项

- Checkpoint AIR fail-open：关单（独立点击 + pi 画面确认）
- mojibake 乱码标题修复：`c2b885a`，独立复现验证通过
- 剧本策划/编辑器"真实删除分镜"：`abef37a0`，代码已核实存在（`data-script-act="delete-shot"` / `data-editor-act="delete-shot"` 均已接到 `deleteWorkspaceShot`）
- Composer 视觉三项字面清单：①③ 已过 pi 内部清单审查，②（100% 全貌）已过；**但大佬指出这批审查从未对照原站，pi 重新拿原站截图比对后判定：画布不是成片墙+连线、Composer 不是贴节点窄胶囊、顶栏多出"剧本策划"、+菜单/编辑器/节点工具条全部对不上原站——原站对标结论是不通过**。今后所有视觉验收必须原站图+现网图同发，不能只审内部清单

## 7. 未完成 / 阻塞项（不得算完成）

1. **六家真实点击生成写回原分镜卡：仍是 0。** 样例 `134923572` 在现口径下：Civitai `strength=null`（官方硬限）、魔搭只认 Hub `owner/repo`（官方硬限）、Fal 曾发现出站 AIR versionId 用了 `2653078` 而样例要求 `3071582`（这是我方 bug，不是官方硬限，web工程师此前未回修复结果）、HF 无对应 LoRA sibling（官方硬限）。四条里三条是产品决策问题（要不要补强度/换 Hub LoRA/换样例），一条是待修 bug。
2. **画布/Composer/编辑器/+菜单/节点工具条与原站结构性不符**（见第 6 节最后一条），这是本轮新发现的最大缺口，规模不小于一次前端重做。
3. **页头 `script` 工作区与项目面板 `story` 工作区未合一**（第 2 节已核实两个 data-workspace 标记仍并存）。
4. **三条旁支分支未合并**，需要大佬按第 4 节拍板。
5. `test_storyboard_graph` 52→14 的落差原因未查。
6. ASTRA 三条点击闸（混合 LoRA 整包拦 / 视频未知能力失败态 / 换模型后 LoRA 不残留）代码已合并（`94e9d89` 等），**但此前一直卡在服务未钉死，从未在稳定服务上做过浏览器点击验证**。现在服务已确认钉死在 `abef37a0`/PID `912973`，具备补测条件。

## 8. 操作要点（避免重复踩坑）

- 只在 `18832` 起服务，只在 `work-repo/civitai-studio-cloud-main` 这个目录，`PORT=18832 nohup python3 server.py > /tmp/civitai-18832.log 2>&1 &`，重启前先杀旧 PID。改 Python 必须重启；静态资源热更新，handler 不热更新。
- 核对现服身份三件套：PID+ppid、`git rev-parse HEAD`、`live JS sha256` 对比 `git show HEAD:static/storyboard.js`，三者对不上就停，报"服务未就绪"，不要往下点。
- 只在 main 上干活，改完立即 commit+push，不攒批，不开新功能分支（`feat/cloud-nodes-poc` 的教训：一开分支就没人记得同步，放了 15 个提交生灰还被当成参照系）。
- 视觉验收：给 pi 必须原站截图+现网截图同发，不划定"只审 X 项"的范围。
