# o26 Fal 换家 + backend dropdown Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use checkbox syntax.

**Goal:** Composer can select Fal (and all enabled backends); after any Civitai import, switching to Fal rewrites foreign serviceIds honestly so outbound is Fal-native — never invent prompt/strength; NEVER 可交付.

**Architecture:** `#backend` syncs from `/api/providers` (all registered, no hasKey filter). `pinFalLoraServiceId` rejects civitai/HF ids (mirror pinHf). Backend onchange clears foreign `#service` and pins `fal-ai/krea-2/turbo/lora` when LoRA chips present.

**Tech Stack:** storyboard.js/html, providers.list_public(), test_storyboard_graph.py

**Spec:** Looper unsupported-param policy; AIImageStudio acceptance lock; Fal first rotate.

## Global Constraints

- NEVER invent/silent-swap params; honest unsupported UI
- Fix real wiring bugs only
- Acceptance: fresh AIImageStudio image only (开发 picks); abandon hinablue/19201654/142210587/136802026 as acceptance
- 魔搭/HF approx LoRA OK per iron rules; Fal uses http LoRA paths
- No curl generate; NEVER 可交付

---

### Task 1: Fal 换家 wiring + dropdown sync

- [x] **Step 1:** pinFal rejects civitai/HF serviceIds
- [x] **Step 2:** backend onchange clears foreign service + ensureFalLoraServiceSelected
- [x] **Step 3:** syncBackendOptionsFromProviders from /api/providers
- [x] **Step 4:** test_v0821o26_fal_swap_dropdown + related tests green
- [ ] **Step 5:** Commit+push o26 + report hash under /workspace/projects/

