# Plan: HF page↑ burn — AIImageStudio 28216105 (o31)

## Goal
After o31 (`5db7c4d`), hard-refresh storyboard, import unused AIImageStudio image `28216105`, switch backend to Hugging Face, confirm service pins to `fal-ai/flux-lora` (flux1 LoRA @816500 strength=0.5), page-click ↑ with **original prompt/params**, capture outbound jobId + writeback evidence. No custom prompt. No review calls. Closed-loop count stays 0 until pack+eyeball.

## Known sample
- Author: AIImageStudio ✓
- LoRA: urn flux1 `@816500` strength **0.5** (REST generation/data)
- Must NOT reuse `28216425` (already burned fail)

## Steps
1. Hard-refresh `http://127.0.0.1:8765/storyboard` — confirm stamp `v0821o31-hf-lora-base`
2. Import `https://civitai.com/images/28216105` (or .red) — keep original prompt; strength visible 0.5 not Missing
3. Backend → Hugging Face; service must become `fal-ai/flux-lora` (not krea-2/turbo)
4. Screenshot composer-before (prompt+LoRA+service)
5. Click ↑ / #send — wait jobId; no silent noop
6. Screenshot outbound/job + card-after-gen
7. Hard-refresh; screenshot card-after-hard-refresh (must differ md5)
8. Ask api对接助手 for outbound field proof; eggbot eyeball if packing

## Anti
- No curl `/api/generate` as acceptance
- No invent strength / silent model swap
- No 「可交付请审」
