# o33 HF Router honest (no Path A score)

## Rule (api+Looper 2026-09-10)
o32 Path A (`backend=huggingface` + `transport=fal`) **does not count as HF**. Fal already scored 1 — no double-count.

## Knife
1. HF + official Fal LoRA sid → Composer honest「Router 不托管，请换家 Fal」; keep chips; no silent Fal-key outbound as HF score path.
2. Real HF↑: Hub mid + HF token via `router.huggingface.co` (no transport=fal).
3. Stamp `v0821o33-hf-router-honest`. Closed-loop 0.
