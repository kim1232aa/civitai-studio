# §7 差距表 P0 开修 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the four biggest Seko gaps on `/storyboard` from `/workspace/docs/review-shots/s7-gap.md` — visible source→result wires, closer shell skeleton, honest text/audio (no fake chrome), i2v first-frame entry like Seko — without empty-test theater or unverified Pass.

**Architecture:** Storyboard canvas (`static/storyboard.js` + CSS/HTML) is the product surface. Wires/drawWires already exist for edges; gap is capture-state often single-card with no visible source→grid edges. Skeleton = Composer/rail/top chrome vs Seko right-rail + multimodal capsules. Text/audio already labeled「未接」— either wire real capability if API supports, or keep honest unfinished without looking finished. i2v already has 缺首帧 gate — add upload/select first-frame entry matching Seko video Composer.

**Tech Stack:** `/workspace/civitai-studio` feat/cloud-nodes-poc; `static/storyboard.js`, `static/storyboard.html`, `static/storyboard-ui.css`; canon `docs/00-IRON-RULES.md` + `docs/review-shots/s7-gap.md`.

## Global Constraints

- Seko = baseline; local = under test. Never claim「已按原标准接入」or closed-loop Pass without evidence.
- No empty-test (no hydrate/hard-refresh theater before fixes).
- Never curl `/api/generate` as acceptance; never invent strength/prompts; never cut API-supported capability to go green.
- t2i 图片生成 is ON — do not label as 未接.
- After each knife: bump stamp, code review Pass before merge, then UI/Critiquito — not before.

---

### Task 1: 连线可见（源→结果）

**Files:** Modify `static/storyboard.js` (drawWires / edges / import promote), possibly CSS stroke visibility

- [ ] **Step 1:** Reproduce why capture state has no visible edge from source asset → shot (read drawWires + edge creation on import/↑)
- [ ] **Step 2:** Ensure after import or writeback, an edge exists and draws (source media node → shot card) when Seko would show 九宫格线
- [ ] **Step 3:** Manual page check screenshot only after code change (not empty hydrate ritual)
- [ ] **Step 4:** Commit + 代码审查员

### Task 2: 骨架更像 Seko（Composer/信息架构）

**Files:** `static/storyboard.html`, `storyboard-ui.css`, Composer dock

- [ ] **Step 1:** Diff against `s7-seko-*.png` / seko-baseline — list concrete chrome deltas (right rail chat, multimodal capsules)
- [ ] **Step 2:** Ship smallest visible IA fixes that close “本地壳” without empty tabs
- [ ] **Step 3:** Review + stamp

### Task 3: 文/音空壳

**Files:** Composer mode chips for 文本/音频

- [ ] **Step 1:** Confirm official API support for text/audio in studio; if unsupported, keep「未接」honest and remove fake affordances that look clickable-complete
- [ ] **Step 2:** If supported, implement real outbound path (no stub chrome)
- [ ] **Step 3:** Review

### Task 4: i2v 首帧入口

**Files:** `static/storyboard.js` i2v dock / first-frame slot

- [ ] **Step 1:** Expose upload/select/canvas-pick first frame like Seko video Composer (beyond red「缺首帧」block)
- [ ] **Step 2:** Ensure outbound still materializes to public/data URL (no bare /out Fail)
- [ ] **Step 3:** Page click path evidence later — after code review, not as empty test

### Task 5: Stop conditions

- Do not burn C维 ↑ until authorized.
- Do not claim sample closed-loop count > 0 until UI+§7 evidence.
