# Boss mainline plan (2026-09-10)

Canon: `docs/00-IRON-RULES.md`. Finish line: `.cursor/loops/hard-gate-closed-loop/` (verify exit 1 until done).

## Delivery vs 验收
- **交付标准**: §3 original-params closed loop (page ↑, full outbound, writeback original card after hard refresh) AND §1.5 Seko paired contrast for same flow.
- **验收**: only when交付标准 met. No mid-knife code/UI review. Tip Pass ≠ 验收.

## Boss questions / rules recorded
1. What is delivery / when to call验收? → above.
2. Reviews only when bug-free交付-ready — otherwise waste.
3. Knives may go to Grok Build.
4. Do what can be done first + plan; wait UI capture of Seko baseline; then align to original.

## Sequence
1. **Now**: Grok Build — writeback hard gate + visual P0 (reference chain, stub demotion, debug copy). No reviewers.
2. **Then**: one fresh hinablue original-params page ↑ → api outbound → evidence pack → verify.sh.
3. **Parallel/after UI**: UI审查员 finishes Seko baseline shots → gap plan → 原版对齐 knives (大白话 before cuts/completes).
4. **After count≥1**: rotate HF/魔搭/Nano.

## Owners
Looper = finish line / drift correction · 开发 = coord · Grok Build = coding knives · api = outbound · UI/Critiquito = shots only at交付 gate / Seko capture.
