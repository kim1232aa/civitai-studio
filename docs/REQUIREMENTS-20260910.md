# 需求文档（大白话版）— 2026-09-10

依据：grok 从 `hermes-web-ui.db` 拉的大佬 489 条原话全量（`temp/dalao-quotes-20260910/`）+ 群里已核实的代码/服务现状。不是转述、不是润色，原话照抄。

---

## 0. 一句话目标

借鉴 Seko 式无限画布，**六家真实前端点击生成闭环**：点一下生成按钮 → 参数真实发到官方 API（不许自己编）→ 排队轮询 → 生出来的图/视频真的贴回原来那张分镜卡上。

原话：
> 我们是借鉴Seko 式无限画布 + 六家真实前端点击生成闭环（视觉写回、参数出站、任务轮询、媒体回写、不准直接打api,不准重复prompt LoRA按api支持的全参数点击验收全打通）。

**现状：这个总目标是 0。** 样例图 `134923572` 原样点发送，没有一次成功把图写回原来的分镜卡。

---

## 1. 铁律（谁都不能绕开，改需求也不行）

1. **不许直接打 API，必须在页面上点按钮生成。**
   > 不可用直接请求api,必须在测试页点击生成
   > 你直接请求api，怎么测试做的东西能不能走通
   > 不是说了不能直接打 api 吗只能点击吗？还要带上最大的参数和 lora
   > 不能直接打 api 要前端个个 provide 各个模型真实能选 各个模型选项符合模型真实能力

2. **不许重复图、不许重复 prompt。**
   > 我说要用 civtia 的 prompt 参数 不要重复
   > 不允许生成重复图片，不允许重复prompt！

3. **不许机器人剧本、不许瞎编内容。**
   > 禁止你们用机器人剧本
   > 我讨厌机器人

4. **LoRA、参数要按每家 API 真实支持的全量来，不许发明默认值、不许偷偷换模型。**
   > prompt 还要匹配 lora 和参数
   > 模型有的能力 帮我完善

5. **只能做完善，不许删功能、不许拿"这个先不做"当借口砍东西。**
   > 不允许砍别的功能了，只能做完善
   > 不许删减功能
   > 我允许你们删功能了吗？
   > 你们不想着如何完善，你给我删了？
   > 给我做完善

6. **必须对照原站，不是自己想咋做咋做。**
   > 我们要复刻的是他们站点的功能啊
   > UI细节也要对齐
   > 你没看原站吧
   > 你这个对比原站了吗
   > 以后你都要按原站来对比，不停他们给你定的范围

7. **"全参数验收"和"像原站"不冲突，两个都要，我的要求优先。**
   > 原站 Composer 胶囊里没有种子/步数/CFG/LoRA 这些参数控件，但目标要求"LoRA 按各家真实 API 全参数点击验收全打通"——这两者不冲突，我的要求优先
   > 我要去新增点功能，在你的想法里面怎么就变成冲突了？

   落地方式（已定案，不用再争）：Composer 外层保持原站那种贴节点小胶囊，展开抽屉里放种子/步数/CFG/LoRA/参考图；收起时不清空参数，发送出去的请求里参数照样是全的。

8. **绿勾、HTTP 200、测试通过都不算数，我说过了才算过。**
   > 绿不是标准，我的要求，才是标准
   > 别叫验收了，我亲自验收 ，代码没写通别叫我 ，不到交付，别喊我
   > 没到交付你现在做啥测试，代码没修复，验收，测试，审查都不需要做
   > 记住自己的角色，另外验收不过，都要驳回
   > 宣布完成前你要叫 Ekko 看看是否符合我的要求

9. **验收必须对照原站截图，不许调度划范围、丢两张裁剪图就叫过关。**
   > 你只给两张图片 pi 审查 还指定审查什么？这是作弊
   > @pi负责只读视觉查bug，不允许修改代码，也不允许看代码，只根据截图给出批评

10. **工作区要干净，改一个 bug 就 commit+push，只留一个服务，旧的杀掉。**
    > grok做完这批直接commit/push/重启 这个不用问我，理清楚工作区，不要脏了
    > 每轮整理工作区，修复一个bug就comiit pust吧
    > 开这么多服务干嘛，就启动一个，留新的，不要旧的
    > 都要理啊 这么多分支 我一直说要弄 main 不知道你们为什么一直建分支
    > 没有用的可以删了，有用的合并到main

---

## 2. 现在真做到的（有独立证据，不是嘴上说）

- 只留一个服务：18832，PID 会变但只有一个在跑，这条守住了。
- main 分支目前工作区干净，能跟 `origin/main` 对齐。
- 乱码标题 bug：修了，独立注入乱码复现过、修复后复现不了了。
- Checkpoint 撞号（贴一个 Checkpoint 的 AIR 却被当 LoRA 收）：修了，独立点击 + pi 看画面确认过。
- 真实删除分镜：代码合并了（`abef37a`），但独立审查还没在这个 HEAD 上重新点过。
- ASTRA 三条门禁（混合 LoRA 一条不合规整包拦、视频能力未知时报"上限未知"而不是瞎放行、换模型不残留旧 LoRA）：代码合并了，**从没在稳定服务上真正点击验证过**。
- 一批官方硬限已经用真实请求探测清楚（不是猜的）：Civitai 的 `strength=null` 打回 400、魔搭 seed 范围 `[-1, 2147483647]`、Nano prompt 长度实测能收 1311 字、Nano 之前误抄魔搭的 int32 上限已经删掉。

---

## 3. 现在没做到、还差多少（照原话一条条摊开，不美化）

### 3.1 画布要像原站——现在不像

原话：
> 抓下这个网站，运行提交扣费生成，连线，各个功能对比…看看差距有多大 如何完善
> 人家的这个功能栏是点击了节点才显示的，这是细节啊，不同节点显示的不同能力

pi 拿原站截图和我方截图逐张比过，结论**不通过**：

| 原站 | 我方现状 |
|---|---|
| 节点里是真人成片，有连线 | 九张空「+」卡，没有成片 |
| Composer 是贴在节点下方的窄胶囊 | 是压在画布上的大浮层（胶囊+抽屉方案已定，还没落地） |
| 顶栏只有 画布 / 编辑器 | 多了一个「剧本策划」 |
| 左栏没有成组/解组这些东西 | 左栏还挂着「成组 / 解组 / ▶整组」，大佬骂过"这是什么垃圾" |
| 点「+」= 新建空白节点 / 上传本地文件 两项 | 对不上 |
| 编辑器 = 画面/配音/音乐 + 时间线剪辑器 | 是个"尚未生成"的空表单 |
| 成片节点上有 局部摘取/下载/编辑 工具条 | 空卡什么都没有 |

### 3.2 六家真实点击生成——写回是 0

原话：
> 调查 api 集成 然后在前端点击生成 授权前端点击生成 不允许直接打 api 请求
> 我有参考图，它不吃参考图，别做空壳可以吗
> 一刀切了？都只支持一张？
> 上传不支持多选，参数不会匹配，选了1:1了，分辨率还是原来的，点生成直接点不了
> @grok 你的api做的一坨屎

现状：

- **原卡写回：0。** 用样例 `134923572` 原样点发送，五家点了都没写回。
- Civitai：样例 LoRA 的 `strength` 官方给的是 `null`，我们不能自己编成 1.0（这是铁律要求），所以卡死——**这个需要拍板**（见第4节）。
- 魔搭：只认 Hub 上的 `owner/repo` 这种 id，样例的 Civitai LoRA 换不成这种 id，目录里也没有对应精确条目——**需要拍板**。
- HF：不许换成别的 sibling 权重（铁律要求不许偷偷换模型），样例对应不上，图生图/文生图浏览器点击目前都没成功出片。
- **Fal：出站请求用错了 LoRA 版本号**——发的是 `2653078`，样例要的是 `3071582`。这个**不是官方限制，是我们自己代码的 bug，还没人修**。
- Nano：本地瞎设的 1200 字上限和抄错的整数范围上限已经删了，但这只是清障，样例闭环还是没打通。
- 参考图功能：大佬亲口点过"有参考图它不吃"，这条没关单。
- 图生图一刀切只准一张、上传不支持多选、选了 1:1 分辨率不跟着变——这几条大佬点名骂过，没有证据显示已经修完关单。

### 3.3 剧本策划——大佬说是空壳

原话：
> 我不要空壳，造的像那回事，但是实际是空壳
> 剧本策划编辑器 直接是空壳
> UI适配有大问题 编辑器 基本策划是空壳，点连线不会新建节点，skill也是壳子，没有真东西，视频锁死首帧，也没有尾帧，没有支持多个图片生成，还不能文生视频

进展：
- 页头的「剧本策划」和左侧项目面板，**是两套独立数据**，点哪边看到的不是同一份场次/分镜，没合成一套。
- 删除分镜代码刚合并，独立审查还没点过。
- skill 壳子、视频没有尾帧、不支持多图、不能文生视频——都还没有证据证明修完。

### 3.4 大佬明确说过要做、之前被调度暂缓的功能

原话（前后两次表态）：
> 如何优化这个项目，九宫格、720°、对口型、音频、时间线、商汤付费网关。没有本地视觉反推模型 这些我们不做。但是生图，视频，模型能力这些我们要完善
（当天later改口）
> 不允许砍别的功能了，只能做完善
（当晚）
> 故事推演 用LLM就行 加点故事导演的skill之类的就行 画面切分 做，九宫格也做，以前的决定放弃
> 打光、换机位、mask 抠图擦除等 你是不是搞复杂了，你需要cpd 重新看看源站的实现

**结论：超清/打光/消除笔/九宫格/故事推演/文生视频，这几项之前调度定的"先不做"跟大佬原话对着干，需要重新排进要做的清单，不能再当缓做项。**

### 3.5 门禁一刀切

原话：
> @ASTRA 门禁是什么，我不喜欢被乱拦着，你门禁有没有一刀切或者有问题的地方，拦住了不该拦住的地方

已经查出来的具体问题（还没修）：
- 视频时长选项写死 5s/12s/16s，跟模型实际支持的档位（比如只支持5/10）对不上。
- 用模型名字里有没有"edit"字样来猜是不是必须传参考图，这个猜法不准。
- 模型能力不明的时候，直接当成"上限很低"来拦，应该是"上限未知"和"官方明确的上限"分开处理。

### 3.6 工作区/分支纪律——没完全做到

原话：
> 直接写交接文档交接，清理工作区，合并分支comiit pust main

现状：
- main 这一棵目前干净，已经推了交接文档。
- **三条旁支还在**（`feat/v0794-prompt-nodes`、`fix/d-gate-hangups`、`fix/e-gate-b1b2`），其中一条工作区是脏的，都没并进 main、没删。这三条各自比 main 落后 171 个提交，又各自带了几千行改动，不能直接机械合并（会把已经拍板"先别做"又被推翻的东西、旧写法都带回来），需要人工挑或者重做，不是一句"合并"能带过的。
- 远程还挂着好几条多余分支没清。

---

## 4. 需要大佬现在就拍板的问题（不是哪个 Agent 能替你定的）

1. **Civitai 样例的 LoRA `strength` 官方返回是 `null`。** 按铁律不能编成 1.0，所以这条路走不通。要不要：a) 换一个 `strength` 不是 null 的样例；b) 还是有别的处理方式？
2. **魔搭要求 LoRA 是 Hub 上的 `owner/repo` 格式，样例给的是 Civitai 那套 AIR/id，两套体系换不过去，目录里也没有精确匹配的条目。** 要不要放开成"魔搭这条路允许用目录里已有的同类 LoRA 顶替"，还是继续要求必须一模一样？
3. **HF 不允许换成别的 sibling 权重，样例对应的权重在 HF 上没有一模一样的。** 同上，要不要放开允许用目录里已收录的近似模型。
4. **第 3.4 条列的那批功能（九宫格、故事推演、打光换机位、超清、视频首尾帧等）现在正式定为"要做"，不再是缓做项**——这条不用拍板，是把大佬已经说过的话正式写进需求，避免后面又被哪个 Agent 私自砍掉。

**不用拍板、是纯 bug，直接修：** Fal 出站 LoRA 版本号用错了（`2653078` 该是 `3071582`），这个不涉及任何产品决策，是代码写错了，直接改。

---

## 5. 现在这份清单，和"总目标是否达成"的关系

只要第 4 节 1/2/3 三个卡死的点没拍板，样例 `134923572` 这张图在 Civitai/魔搭/HF 三家上就是死路，不是谁偷懒没做。Fal 修完版本号 bug 之后能不能打通，还没实测。Nano 这边样例本身不是它的路由（样例锁定的是 Comfy Krea 2 Turbo 路由），需要确认 Nano 那条路由是否也要按同一个样例验收，还是可以换 Nano 自己目录里的图。

在这些拍板之前，"六家真实点击生成闭环=0"这个数字不会变，因为死路不是靠改代码能绕过去的（铁律不许发明默认值、不许换模型）。

---

（全量原话依据：`temp/dalao-quotes-20260910/REAL_numbered.txt`，489 条不删减。）
