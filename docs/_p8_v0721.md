# P8 QA — v0721 (human-QA pass, click-only)

Method: closed extra 8765 tabs, opened ONE new tab to http://127.0.0.1:8765/, hard-refreshed (Ctrl+Shift+R).
Screenshots captured off display :9 (ffmpeg x11grab), stored in docs/review-shots/.

## 1. Version
- **v0721: YES** — header shows "Civitai Studio v0721" after hard refresh. Shot: `review-shots/p8-v0721.png`.

## 2. Import
- `https://civitai.red/images/139791102` imported on Civitai backend → status "已导入参数，自己点生成。" OK.
- On Hugging Face the same URL was typed + 导入 clicked; field cleared but **no "已导入参数" confirmation** was shown (status stayed "Hugging Face 已加载 8 个模型。自己点生成。").

## 3. Fal — fal-ai/z-image/turbo (model "Z Image Turbo" selected, tooltip confirmed)
- Clicked 生成. Captured ~40 consecutive full-screen frames spanning the click (~0.3 s apart).
- **Button NEVER showed 提交中… — it stayed 生成** (only the hover highlight changed). Repeated twice, same result. **FAIL.**
- Shot: `review-shots/p8-fal-clicked.png`.
- After ≤2 min: gallery still **成片 4 张**, **no new image**. Shot: `review-shots/p8-fal-after.png`. **FAIL.**

## 4. Hugging Face — Z-Image Turbo
- Clicked 生成; 40-frame capture: **button stayed 生成, never 提交中…**. **FAIL.** Shot: `review-shots/p8-hf-clicked.png`.
- After ≤2 min: still **成片 4 张**, no new image. Shot: `review-shots/p8-hf-after.png`. **FAIL.**

## 5. 魔搭 AI — Tongyi-MAI/Z-Image-Turbo
- Model selected ("已选 Z-Image-Turbo"), but the run could not be completed cleanly: mid-test the app **rebuilt/reloaded itself to v0730 then v0731**, the layout reflowed (生成 button moved off the visible area), the import field was cleared, and the backend **switched itself back to Fal** without any click from me.
- No 提交中… observed; **no new 成片** (still 4 张). Shots: `review-shots/p8-ms-clicked.png`, `review-shots/p8-ms-after.png`. **INCONCLUSIVE / FAIL.**

## Environment instability observed (important)
During the session, without any action from me:
- extra browser tabs opened by themselves (1 → 4 tabs),
- the app jumped from 图片 to 工作流 tab and auto-imported a workflow,
- the backend switched between Fal / 魔搭 AI on its own,
- the version string changed v0721 → v0730 → v0731 (live rebuilds/hot reload).
The desktop was therefore NOT exclusive; v0721 results above are from before the rebuilds.

## Verdict
- v0721 present: **YES**
- 提交中… state on click: **NO** for Fal, HF; not observed for 魔搭 AI
- New 成片 image: **NO** for all three backends (gallery stayed at 4 张)
