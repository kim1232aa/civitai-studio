# 本文件作废

页 ↑ 铁律只在 `docs/REQUIREMENTS-20260910.md`：

- 「不可用直接请求api,必须在测试页点击生成」
- 「不允许生成重复图片，不允许重复prompt」
- 「还要带上最大的参数和 lora」
- 样例 `134923572`；Civitai `strength=null` / 魔搭 Hub / HF sibling **未拍板**
- 闭环现状是 0，绿勾不算过

**hinablue 作者页和帖 id 表是 agent helper，不是铁律。** 不要在本路径写六家轮次、密钥探测、截图包步骤。

密钥只写本机 `~/.config/<house>/token`，不进仓。只留一个 `python3 server.py`。
