# v0794：完整提示词节点

日期：2026-09-05
约束：执行仍走 `/api/graph/compile` + `/api/generate`。不接商汤。不入库 JWT。不劫持 `/`。

## 合同

图资产 → 反推提示词（画布上可编辑的文本节点）→ 生图卡。
`@角色名` 只在已连线时归一成 `@图片N`。

## 落地

- 提示词卡是真正的 textarea，卡片上直接改，和底栏双向同步
- 拖动只改坐标，不拆掉输入光标
- 反推写出完整提示词：主体 / 外观 / 服装 / 场景 / 光线 / 构图 / 画面 / 运镜 / 约束 / 负面
- 反推优先读 `/api/caption` 和 `out/*.json` sidecar，没有就用角色库圣经，绝不抛错
- 文本节点「生图」建或复用分镜，接线 text→shot，并把上游图接到分镜，再走现有 t2i / i2i
- `@` 在卡片和底栏都能用，角色库标题进提示词并连线
- 六个镜头各有独立提示词，全部 `image → text → shot`
- STORE `nl-storyboard-v0794`，相同 URL 的苹果缓存直接丢弃

## 仍灰

九宫格、720°、对口型、音频、时间线、商汤网关。
