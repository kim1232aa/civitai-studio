# Civitai Studio 现状（大白话）· 2026-09-11

> 老板原话只认 `docs/REQUIREMENTS-20260910.md`。本文只记现状。agent 口径不是铁律。

## 总目标（原话）

> 我们是借鉴Seko 式无限画布 + 六家真实前端点击生成闭环（视觉写回、参数出站、任务轮询、媒体回写、不准直接打api,不准重复prompt LoRA按api支持的全参数点击验收全打通）。

**现状：这个总目标是 0。** 样例 `134923572` 原样点发送，没有一次成功把图写回原来的分镜卡。

## 源站对照（第一条要求，还没做成）

Seko 无限画布是基准。未登录只见登录墙 / 营销页，**不算对照通过**。本地文件名带 `seko-` 的图里，除登录墙外都是 Night Lab，不是源站画布。没有成对截图之前，不许喊「像 Seko」。

公开材料写过：角色/场景/道具/分镜连线、点节点才出能力、自动布局、编辑器时间线、九宫格/720°/故事推演。本站还没有这些对照证据。

## 还没拍板（REQUIREMENTS §4）

1. Civitai 样例 LoRA `strength=null`，不能编 1.0。换样例还是别的办法，等老板。
2. 魔搭只要 `owner/repo`，样例 AIR 换不过去。是否允许目录近似，等老板。
3. HF 无一模一样 sibling。是否允许近似，等老板。

Fal `2653078`→`3071582` 是代码 bug，不用拍板。
九宫格 / 故事推演 / 打光 / 超清 / 视频首尾帧：老板说只能完善、不许砍，定为要做。调度「C 组本轮不做」作废。

## 不是铁律

- **hinablue** 及 `142373903` 等帖：agent 自备检索，不是老板指定唯一作者页。
- 「Fal 空画布 Seko 基线不计分」：agent 发明。空「+」卡对照原站是不通过，不是不计分。
- 「已拍板：换 strength 样例 / 允许魔搭 HF 近似」：agent 替定，作废。
- `docs/api-usage/PAGE-UP-PLAYBOOK.md`：agent 操作备忘，不是需求。

## 代码层（不是交付）

权威 id：`civitai` / `fal` / `huggingface` / `modelscope-ai` / `modelscope-cn` / `nano-gpt`。

- Civitai LoRA：Klein/Comfy/LTX2 = `{air:strength}`；Dev/WAN/Hunyuan = `[{air,strength}]`
- Fal：duration / maxRefs 只抄该端点 OpenAPI
- HF：无 `loras` 键；seed 不 wrap
- 魔搭：seed -1 省略；Hub `owner/repo`；AI/CN 不交叉
- Nano：无 promptMax=1200；无 `loras` 键
- 空 backend 不得默认 `nano-gpt`
- 出站仍须页面点 ↑，禁止 curl `/api/generate` 冒充

这是匹配层。**不是闭环。**

## 总闸

产品闭环计数仍是 **0**。tip 合 main ≠ 交付。未出成对截图前，不要叫 UI/代码「产品验收」。

## 怎么跑

只启动一个 `python3 server.py`。端口以进程为准。

```bash
python3 server.py
PYTHONPATH=. python3 scripts/test_o58_official_shapes.py
PYTHONPATH=. python3 scripts/test_o58_writeback_node.py
node scripts/test_o58_composer_caps.js
```
