# Civitai Studio 现状（大白话）· 2026-09-11 · 交接

> 给接班人看的。铁律：`docs/00-IRON-RULES.md` + `docs/api-usage/**`。
> **六家产品闭环 = 0，不能喊验收。匹配完善 ≠ 闭环。**

---

## 一句话

**main HEAD 以 Git 为准（o57 六家 catalog stamp + o58 官方形单测 + 页↑手册）。** Composer 按 `ctx.item` 能力显框；LoRA hint 分六家表，禁止 `NANO.concat(FAL)`。闭环仍计 **0**，直到截图验收人只看图说通过。

---

## Git / main

- 只写 `main`，不开分支（`docs/00-IRON-RULES.md` §8）
- stamp：`v0821o57-item-match` / `v0821o57-six-lora-hints`
- 密钥只在 `~/.config/<house>/token`，不进仓
- `docs/review-shots/` gitignore；空画布 Seko 基线不计分
- 页↑步骤：`docs/api-usage/PAGE-UP-PLAYBOOK.md`

### 近期 tip（已在 main）

| tip | 白话 |
|---|---|
| o57 | 六家 catalog stamp + Composer 读 item caps + 分家 LoRA hint |
| o58 shapes | `scripts/test_o58_official_shapes.py` 锁官方出站形，禁 `/api/generate` |
| o58 T7–T9 | Composer caps 单测、hinablue 导入夹具、写回原卡单测 |

---

## 六家匹配（代码层）

权威 id：`civitai` / `fal` / `huggingface` / `modelscope-ai` / `modelscope-cn` / `nano-gpt`。

- Civitai LoRA：Klein/Comfy/LTX2 = `{air:strength}`；Dev/WAN/Hunyuan = `[{air,strength}]`；Fal-Krea 拒 LoRA
- Fal：duration / maxRefs 只抄该端点 OpenAPI
- HF：无 `loras` 键；seed 不 wrap；hint 禁止 `fal-ai/*`
- 魔搭：seed -1 省略；Hub `owner/repo`；AI/CN 不交叉
- Nano：无 promptMax=1200；无 `loras` 键；`z-image-turbo-lora` 是启发式 id
- 空 backend 不得默认 `nano-gpt`

这是匹配层。**不是闭环。**

---

## 作废 / 别再捡

- Fal 空画布 Seko 基线 → 不计分
- 工作室画廊生成、不是分镜原卡写回 → 作废
- curl `/api/generate`、改短 prompt、发明 strength → 禁止当验收
- 固定只用 `134923572` 当唯一导入帖 → 禁止

---

## 页↑还没做的

按 `PAGE-UP-PLAYBOOK.md`：hinablue 换图导入 → Composer 全参 → 页面点 ↑ → 原卡写回 → 硬刷。验收人只看图。本文件不宣布 Pass。

可用帖（每次换，不要当唯一夹具）：

- https://civitai.red/images/142373903（prompt 内含 `<lora:Purple_Graphics_KR2:1>`）
- https://civitai.red/images/142373894
- https://civitai.red/images/142373085

某家官方不接该 LoRA 形 → 换帖，不发明 strength。

---

## 总闸

产品闭环计数仍是 **0**。tip 合 main ≠ 交付。未出成对截图前，不要叫 UI/代码「产品验收」。

---

## 本地怎么跑

```bash
python3 server.py   # 端口不钉死，记 PID + HEAD
# 审计：out/generate-audit.jsonl
# 分镜图：data/storyboard_graph.json
PYTHONPATH=. python3 scripts/test_o58_official_shapes.py
PYTHONPATH=. python3 scripts/test_o58_import_hinablue.py
PYTHONPATH=. python3 scripts/test_o58_writeback_node.py
node scripts/test_o58_composer_caps.js
```
