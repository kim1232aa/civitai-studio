# Civitai Studio 现状（大白话）· 2026-09-11 · 交接

> 给接班人看的。铁律：`docs/00-IRON-RULES.md` + `docs/api-usage/**`。
> **六家产品闭环 = 0，不能喊验收。匹配完善 ≠ 闭环。**

---

## 一句话

**main 已推到 `804e33b`（o54b：LoRA rematch 走全量 `/api/catalog` roster + 导入芯片自动匹配）。** 六家优先端点 hint 尚未齐（只有 Nano/Fal 明确表；Civitai/HF/魔搭 AI/魔搭 CN 仍盲选 `supportsLora=true`）。Fal 曾有出站+写回痕迹但因空画布 Seko 基线作废不计分。Nano `30406474` 画布闭环未收完。

---

## Git / main

- `main` = `origin/main` = **`804e33b`** — `fix(storyboard): o54b LoRA rematch from full catalog roster`
- stamp：`v0821o54b-lora-roster-rematch` / cache `20260911-o54blorarosterrematch`
- 正式代码已对齐 remote；`.jspace` / `.cursor` / 旧 plan 草稿**不进仓**
- `docs/review-shots/` 被 gitignore（本地证据包若在，勿当已计分闭环）

### 近期 tip（已在 main）

| tip | hash（约） | 白话 |
|---|---|---|
| o51 | 修 expanded 重复声明 | JS 不再整页挂死 |
| o52 | `_pendingService` 逃过 i2i 过滤 | 导入模型不丢 |
| o53→o53d | `5b3142e` | 参考超 cap 一键匹配；全量 roster |
| o54 | `e07e8da` | `supportsLora` 同 backend 自动匹配；禁静默丢芯片 |
| o54b | **`804e33b`** | 全量 roster 拉取 + 导入芯片自动 rematch；Nano/Fal 有优先 hint |

---

## 六家匹配（还没齐）

权威 id（`docs/api-usage/README.md`）：`civitai` / `fal` / `huggingface` / `modelscope-ai` / `modelscope-cn` / `nano-gpt`。Magao 不是第六家，是魔搭 `Qwen-Image-Edit-2509`。

o54b `pickLoraHintFromPool` 只有：

- Nano：`flux-lora` / `flux-2-dev-lora` / `krea-v2/turbo-lora`（缺 inventory 实测的 `z-image-turbo-lora`）
- Fal：`fal-ai/flux-lora` / `fal-ai/flux-lora/image-to-image`
- 其他家：`NANO.concat(FAL)` 再盲选 `supportsLora=true`
- catalog 拉取 fallback 空 backend 时默认 `nano-gpt`（隐患）

还缺（o55，**未落地**）：

- Civitai / HF / 魔搭 AI / 魔搭 CN 本家优先端点表
- Nano 导入分支（无云端反查；匹配只在已在 `nano-gpt` 时触发）
- HF 不得 hint `fal-ai/*lora`（o33 诚实 400）
- 魔搭只认 Hub `owner/repo`；AI 失败禁止改走 CN

---

## 作废 / 别再捡

- Fal `32050872`：出站+写回做过，但 Seko 基线是**空画布** → eggbot 作废，**Fal tip 不计**
- Nano gallery job `aac802664911`：旧工作室画廊生成，**不是**分镜原卡写回 → 作废
- curl `/api/generate`、改短/自定义 prompt、发明 strength → 禁止当验收

---

## P0 接班从这接着干

1. **o55 六家 hint 表 + Nano 导入分支**（匹配完善，不算闭环）
   - `pickLoraHintFromPool` 按 backend 分表，禁止 `NANO.concat(FAL)` 兜底到别家
   - catalog fetch 禁止空值默认 `nano-gpt`
   - Nano hint 补 `z-image-turbo-lora`；HF 只 Hub mid；魔搭只 `owner/repo`
   - 不自动跨家切 backend；不发明 id/strength；不静默丢芯片
2. **Nano 帖 `30406474`**（AIImageStudio 未用）：seed 42（int32 OK），promptLen≈487，LoRA Analog Film `832587`@0.7（以 `/api/import-image` 为准）
   - **只认画布 Composer ↑** → 出站原参 → 写回原分镜卡 → 硬刷还在
   - 挂 **Nano `flux-lora`**（o54b 应能从 Seedream 等非 LoRA 模型自动匹配过来）
   - Nano 分辨率必须选目录 token（如 `768x1024`），不能自由宽高
   - 证据包 + **非空** Seko 对照（用 `s7-seko-03-composer` / `s7-seko-06-writeback`，禁止空画布基线）
3. 换家：**魔搭 Edit-2509** 未用帖 → **真 HF** → **Civitai**（别再只啃 Fal）
4. 源站对照：Seko `https://seko.sensetime.com/infinite-canvas?...` 同流程成对截图；缺对照不算交付
5. 验收图源：每次换没用过的  
   `https://civitai.red/user/AIImageStudio/images`

---

## 总闸

产品闭环计数仍是 **0**。tip 合 main ≠ 交付。未齐六家匹配 + 有效 Seko 对照前，不要叫 UI/代码「产品验收」。

---

## 本地怎么跑

```bash
cd /workspace/civitai-studio
python3 server.py   # http://127.0.0.1:8765
# 审计：out/generate-audit.jsonl
# 分镜图：data/storyboard_graph.json
```

---

## 交接时工作区

- 已 push o54b；STATUS 本次只纠正过期 HEAD，**o55 代码未落地**
- 杂物目录（`.jspace/`、`.cursor/`、旧 plan）可删，勿 commit
