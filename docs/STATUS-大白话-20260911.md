# Civitai Studio 现状（大白话）· 2026-09-11

> 给老板看的。技术细节少写，只说「做到哪了 / 还差啥 / 下一步干啥」。
> 铁律仍以 `docs/00-IRON-RULES.md` 为准。

---

## 一句话

**代码 tip 推到 main 了（含 o53c 一键匹配），Fal 有一家真出站+硬刷写回证据；六家产品闭环还是 0，不能喊验收。**

---

## Git / main

- 当前 `main` = `origin/main` = `a803ddf`（o53c：补上「一键匹配」点击 + 防 LoRA 又钉回 flux-lora）
- 本地没有未推送的正式代码改动
- 未提交的是杂物（`.jspace/`、`.cursor/`、旧 plan 草稿），**故意不进仓库**
- 证据截图目录被 `.gitignore` 忽略（本地有 `fal-32050872` 包，verify.sh 本地 exit 0）

---

## 已经做成的（不算六家闭环完成）

| 事 | 白话说明 |
|---|---|
| o51 | 修了 JS 挂死（重复声明），画布按钮又能点 |
| o52 | 导入后模型不会被 i2i 目录过滤弄丢 |
| o53 / o53b / o53c | 参考图超上限时「一键匹配」到够容量的模型；**o53c 刚合上，浏览器硬刷验收还在跑** |
| Fal 帖 `32050872` | 原 prompt（881 字）+ Analog Film@0.7 真 page↑；api 出站 Pass；成片写回分镜4，硬刷还在；本地证据包 verify 过 |
| 多家灌满烟测 | Nano/Fal/魔搭/Civitai 参考图灌满做过，**只算接线烟测，不算原帖闭环** |
| Civitai 等 | 有过自动写回/出站进展，但仍缺「六家都齐」 |

---

## 总闸：还差什么才算「做好了」

老板要的不是 tip 堆叠，是：

1. **六家**（至少 Civitai / Fal / HF / 魔搭 / Nano…）各自：
   - 从未用过的 **AIImageStudio** 原帖
   - **页面点 ↑**（禁止 curl）
   - **原 prompt + 原参数 + LoRA** 出站对得上
   - 成片写回**原分镜卡**，硬刷还在
   - **源站 Seko 对照**成对截图
2. 现在：**产品闭环计数 = 0**（Fal 这一家证据有了，也不等于六家齐；也不能单家交差完事）

---

## 代办清单（按优先）

### P0 · 必须先做（挡住「能交付」）

1. **验 o53c**：硬刷后点「一键匹配」，3/1 应跳到够容量模型（优先 flux-2/edit），**不能静默砍参考线**
2. **换家继续烧**（别再只啃 Fal）：
   - **Nano**：找 AIImageStudio 未用帖，种子必须能进 int32（大种子会炸）
   - **魔搭 Edit-2509**：未用帖原参 page↑ + 写回（含 `119689290` 一类，需对齐 service）
   - **真 HF**：别用 Fal 冒充 HF
   - **Civitai**：补齐可计分的原帖闭环证据包（之前有过半截）
3. **源站对照**：Seko 同流程成对截图（导入→Composer→参数/LoRA→↑→写回），缺这个不算交付
4. **六家都齐之前**：不叫 UI/代码「产品验收」；tip 审最多标「代码 tip 审」

### P1 · 重要但可排后

- Composer 贴底自适应 / 跟 Seko 差距（空壳、能力半槽、双↑、参考链等视觉债）
- hydrate / 多开桌面互相覆盖写回（已有部分修，仍要盯）
- `/api/search` 路由、seed 范围提示、LoRA 搜索等边角
- cloud-nodes / LiteGraph 抛光（老板说过优先级在画布 API 之后）

### 已作废 / 别再捡

- Fal `28978604` 证据包（卡面和出站对不上）——不计分
- 空 prompt / 短 SFW 自定义 prompt 的「过关」——禁止
- curl `/api/generate` 冒充验收——禁止
- 同家族会炸的 LoRA（如某些 krea Midjourney 权重 Fail）——换帖，别硬撞

---

## 验收作者页（每次换没用过的图）

`https://civitai.red/user/AIImageStudio/images`

---

## 现在不该做的事

- 不要喊「可交付请审」
- 不要为了绿测砍功能或静默丢参考图
- 不要再堆无关 tip；只修挡硬闸证据的 bug
- 不要把「出站 Pass」或「verify 一个包 exit 0」说成六家闭环完成

---

## 下一步（我这边默认顺序）

1. 收完 o53c 硬刷点按结果（Pass/Fail 有截图再说）
2. 开 **Nano** 未用帖（int32 seed）真 page↑
3. 再开 **魔搭** 未用帖
4. 边烧边补 Seko 对照；六家齐了再谈交付审查

## 更正（eggbot 2026-09-11）

- `fal-32050872` **作废不计分**：Seko 基线是空画布，对照 Fail。
- Fal tip **收回**（不再计 1）。写回也许真，没有合格源站对照就不算。
- 六家闭环仍 **0**；下一刀仍是非 Fal（Nano 未用帖）。
