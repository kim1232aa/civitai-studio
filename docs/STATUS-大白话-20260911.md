# Civitai Studio 现状（大白话）· 2026-09-11 · 交接

> 给接班人看的。铁律：`docs/00-IRON-RULES.md` + `docs/api-usage/**`。
> **六家产品闭环 = 0，不能喊验收。**

---

## 一句话

**main 已推到 `e07e8da`（含 o54 LoRA `supportsLora` 匹配）。Fal 曾有出站+写回证据但因空画布 Seko 基线作废不计分。Nano `30406474` 画布闭环未收完。工作区杂物已清，可交接。**

---

## Git / main

- `main` = `origin/main` = **`e07e8da`** — `fix(storyboard): o54 LoRA capability rematch via supportsLora`
- stamp：`v0821o54-lora-capability-match` / cache `20260911-o54loracapabilitymatch`
- 正式代码已干净对齐 remote；`.jspace` / `.cursor` / 旧 plan 草稿**不进仓**
- `docs/review-shots/` 被 gitignore（本地证据包若在，勿当已计分闭环）

### 近期 tip（已在 main）

| tip | hash（约） | 白话 |
|---|---|---|
| o51 | 修 expanded 重复声明 | JS 不再整页挂死 |
| o52 | `_pendingService` 逃过 i2i 过滤 | 导入模型不丢 |
| o53→o53d | `5b3142e` | 参考超 cap 一键匹配；全量 roster |
| o54 | **`e07e8da`** | 有 LoRA 芯片 + 当前模型 `supportsLora=false` → 同 backend 自动匹配（Nano 优先 `flux-lora`）；无芯片则藏 LoRA 区；禁静默丢芯片 |

---

## 作废 / 别再捡

- Fal `32050872`：出站+写回做过，但 Seko 基线是**空画布** → eggbot 作废，**Fal tip 不计**
- Nano gallery job `aac802664911`：旧工作室画廊生成，**不是**分镜原卡写回 → 作废
- curl `/api/generate`、改短/自定义 prompt、发明 strength → 禁止当验收

---

## P0 接班从这接着干

1. **Nano 帖 `30406474`**（AIImageStudio 未用）：seed 42（int32 OK），promptLen≈487，LoRA Analog Film `832587`@0.7（以 `/api/import-image` 为准）
   - **只认画布 Composer ↑** → 出站原参 → 写回原分镜卡 → 硬刷还在
   - 挂 **Nano `flux-lora`**（o54 应能从 Seedream 等非 LoRA 模型自动匹配过来）
   - Nano 分辨率必须选目录 token（如 `768x1024`），不能自由宽高
   - 证据包 + **非空** Seko 对照（用 `s7-seko-03-composer` / `s7-seko-06-writeback`，禁止空画布基线）
2. 换家：**魔搭 Edit-2509** 未用帖 → **真 HF** → **Civitai**（别再只啃 Fal）
3. 源站对照：Seko `https://seko.sensetime.com/infinite-canvas?...` 同流程成对截图；缺对照不算交付
4. 验收图源：每次换没用过的  
   `https://civitai.red/user/AIImageStudio/images`

---

## 总闸

产品闭环计数仍是 **0**。tip 合 main ≠ 交付。未齐六家 + 有效 Seko 对照前，不要叫 UI/代码「产品验收」。

---

## 本地怎么跑

```bash
cd /workspace/civitai-studio
python3 server.py   # http://127.0.0.1:8765
# 审计：out/generate-audit.jsonl
# 分镜图：data/storyboard_graph.json
```

---

## 交接时工作区

- 已 push o54；接班人 `git pull` 即可
- 杂物目录（`.jspace/`、`.cursor/`、旧 plan）可删，勿 commit
